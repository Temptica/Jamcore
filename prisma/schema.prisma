// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native"]
}

datasource db {
  provider = "postgresql"
}

model User {
  id                Int      @id @default(autoincrement())
  slug              String   @unique
  name              String
  email             String?
  password          String
  profilePicture    String?
  bannerPicture     String?
  profileBackground String?
  short             String?
  bio               String?
  pronouns          String?
  links             String[] @default([])
  linkLabels        String[] @default([])
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  mod               Boolean  @default(false)
  admin             Boolean  @default(false)
  twitch            String?
  twitchUserId      String?   @unique @map("twitch_user_id")
  twitchLinkedAt    DateTime? @map("twitch_linked_at")
  tenantId          String?  @map("tenant_id")

  // Relations
  posts                         Post[]                  @relation(name: "PostToAuthor")
  likes                         Like[]
  comments                      Comment[]
  jams                          Jam[]
  reports                       Report[]                @relation(name: "ReportToUser")
  createdReports                Report[]                @relation(name: "ReportToReporter")
  assignedReports               Report[]                @relation(name: "ReportToAssignee")
  themeSuggestions              ThemeSuggestion[]       @relation(name: "SuggestionToUser")
  themeVotes                    ThemeVote[]             @relation(name: "VoteToUser")
  themeVotes2                   ThemeVote2[]            @relation(name: "Vote2ToUser")
  gamePageTracks                GamePageTrack[]         @relation(name: "GamePageTrackToComposer")
  gamePageTrackCredits          GamePageTrackCredit[]   @relation(name: "GamePageTrackCreditToUser")
  trackTimestampComments        TrackTimestampComment[] @relation(name: "TrackTimestampCommentToAuthor")
  scores                        Score[]                 @relation(name: "ScoreToUser")
  ghosts                        Ghost[]                 @relation(name: "GhostToUser")
  data                          Data[]                  @relation(name: "DataToUser")
  events                        Event[]
  primaryRoles                  TeamRole[]              @relation(name: "PrimaryRolesToUsers")
  secondaryRoles                TeamRole[]              @relation(name: "SecondaryRolesToUsers")
  teams                         Team[]
  ownedTeams                    Team[]                  @relation(name: "TeamsToOwner")
  teamApplications              TeamApplication[]       @relation(name: "TeamApplicationsToUser")
  teamInvites                   TeamInvite[]            @relation(name: "TeamInvitesToUser")
  ratings                       Rating[]                @relation(name: "RatingToUser")
  trackRatings                  TrackRating[]           @relation(name: "TrackRatingToUser")
  commentReactions              CommentReaction[]
  gamePageAchievements          GamePageAchievement[]   @relation(name: "GamePageAchievementToUsers")
  achievementUnlocks            AchievementUnlock[]     @relation(name: "AchievementUnlockToUser")
  streamerGameAlerts            StreamerGameAlert[]     @relation(name: "StreamerGameAlertToUser")
  receivedNotifications         Notification[]          @relation(name: "NotificationToRecipient")
  sentNotifications             Notification[]          @relation(name: "NotificationToActor")
  PostReaction                  PostReaction[]
  reactionsAuthored             Reaction[]              @relation(name: "ReactionToArtist")
  emotePrefix                   String?
  userEmotes                    Reaction[]              @relation(name: "ReactionToUserOwner")
  reactionsUploaded             Reaction[]              @relation(name: "ReactionToUploader")
  recommendedGames              Game[]                  @relation(name: "UserRecommendedGames")
  recommendedPosts              Post[]                  @relation(name: "UserRecommendedPosts")
  recommendedTracks             GamePageTrack[]         @relation(name: "UserRecommendedTracks")
  documentationDocuments        DocumentationDocument[]
  pressKitMedia                 PressKitMedia[]
  recommendedGameOverrideIds    Int[]                   @default([])
  recommendedGameHiddenIds      Int[]                   @default([])
  recommendedTrackOverrideIds   Int[]                   @default([])
  recommendedTrackHiddenIds     Int[]                   @default([])
  hideRatings                   Boolean                 @default(false)
  autoHideRatingsWhileStreaming Boolean                 @default(false)
  messageRequestPolicy           MessageRequestPolicy   @default(EVERYONE)
  siteTheme                      String?
  quiltSubmissions              QuiltSubmission[]       @relation(name: "QuiltSubmissionToAuthor")
  quiltRemovals                 QuiltSubmission[]       @relation(name: "QuiltSubmissionToRemover")
  quiltVotes                    QuiltVote[]             @relation(name: "QuiltVoteToUser")
  conversationsCreated          Conversation[]          @relation(name: "ConversationCreator")
  conversationMemberships       ConversationMember[]    @relation(name: "ConversationMemberUser")
  conversationInvitesSent       ConversationMember[]    @relation(name: "ConversationMemberInviter")
  conversationMessages          ConversationMessage[]   @relation(name: "ConversationMessageSender")
  webBuilds                     WebBuild[]               @relation(name: "WebBuildToOwner")
  usersBlocked                  UserBlock[]              @relation(name: "UserBlockBlocker")
  blockedByUsers                UserBlock[]              @relation(name: "UserBlockBlocked")

  @@index([tenantId])
}

enum Priority {
  HIGH
  MEDIUM
  LOW
}

enum PageVersion {
  JAM
  POST_JAM
}

enum QuiltSubmissionStatus {
  PENDING
  ACCEPTED
  REJECTED
  REMOVED
  USER_DELETED
}

enum QuiltSubmissionKind {
  PIXELS
  RESIZE
}

model TagCategory {
  id        Int      @id @default(autoincrement())
  name      String   @unique
  priority  Int      @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relation
  tags Tag[]
}

model Tag {
  id          Int      @id @default(autoincrement())
  name        String   @unique
  description String?
  autoRegex   String?
  alwaysAdded Boolean  @default(false)
  priority    Priority @default(MEDIUM)
  modOnly     Boolean  @default(false)
  icon        String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  gameTag     Boolean  @default(true)
  postTag     Boolean  @default(true)

  // Relations
  posts      Post[]
  games      Game[]       @relation(name: "GamesToTags")
  gamePages  GamePage[]   @relation(name: "GamePagesToTags")
  category   TagCategory? @relation(fields: [categoryId], references: [id])
  categoryId Int?
}

model CustomTag {
  id        Int      @id @default(autoincrement())
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relations
  posts Post[]
}

model Post {
  id                 Int       @id @default(autoincrement())
  slug               String?   @unique
  title              String
  content            String
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt
  editedAt           DateTime?
  deletedAt          DateTime?
  removedAt          DateTime?
  sticky             Boolean   @default(false)
  draftStatus        String    @default("published") @map("draft_status")
  scheduledPublishAt DateTime? @map("scheduled_publish_at")
  previewToken       String?   @map("preview_token")
  tenantId           String?   @map("tenant_id")

  // Relations
  author             User           @relation(fields: [authorId], references: [id], name: "PostToAuthor")
  authorId           Int
  tags               Tag[]
  customTags         CustomTag[]
  comments           Comment[]
  likes              Like[]         @relation(name: "LikeToPost")
  reports            Report[]       @relation(name: "ReportToPost")
  reactions          Reaction[]     @relation(name: "PostToReactions")
  postReactions      PostReaction[]
  notifications      Notification[] @relation(name: "post")
  recommendedByUsers User[]         @relation(name: "UserRecommendedPosts")

  @@index([tenantId, createdAt])
}

enum DocumentationSection {
  DOCS
  PRESS_KIT
}

model DocumentationDocument {
  id        Int                  @id @default(autoincrement())
  slug      String               @unique
  title     String
  content   String
  icon      String               @default("book")
  order     Int                  @default(0)
  section   DocumentationSection
  createdAt DateTime             @default(now())
  updatedAt DateTime             @updatedAt
  editedAt  DateTime?

  author   User @relation(fields: [authorId], references: [id])
  authorId Int
}

model PressKitMedia {
  id        Int      @id @default(autoincrement())
  image     String
  altText   String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  author   User @relation(fields: [authorId], references: [id])
  authorId Int
}

enum ReactionScope {
  GLOBAL
  USER
  GAME
}

model Reaction {
  id          Int           @id @default(autoincrement())
  slug        String        @unique
  image       String
  artist      String?
  artistId    Int?
  uploaderId  Int?
  scopeType   ReactionScope @default(GLOBAL)
  scopeUserId Int?
  scopeGameId Int?
  createdAt   DateTime      @default(now())
  updatedAt   DateTime      @updatedAt

  // Relations
  artistUser       User?             @relation(fields: [artistId], references: [id], name: "ReactionToArtist")
  uploaderUser     User?             @relation(fields: [uploaderId], references: [id], name: "ReactionToUploader")
  ownerUser        User?             @relation(fields: [scopeUserId], references: [id], name: "ReactionToUserOwner")
  ownerGame        Game?             @relation(fields: [scopeGameId], references: [id], name: "ReactionToGameOwner")
  posts            Post[]            @relation(name: "PostToReactions")
  postReactions    PostReaction[]
  commentReactions CommentReaction[]
}

model PostReaction {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())

  // Relations
  post       Post     @relation(fields: [postId], references: [id])
  postId     Int
  reaction   Reaction @relation(fields: [reactionId], references: [id])
  reactionId Int
  user       User     @relation(fields: [userId], references: [id])
  userId     Int

  @@unique([postId, reactionId, userId])
}

model CommentReaction {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())

  comment    Comment  @relation(fields: [commentId], references: [id])
  commentId  Int
  reaction   Reaction @relation(fields: [reactionId], references: [id])
  reactionId Int
  user       User     @relation(fields: [userId], references: [id])
  userId     Int

  @@unique([commentId, reactionId, userId])
}

model Comment {
  id        Int       @id @default(autoincrement())
  content   String
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  editedAt  DateTime?
  deletedAt DateTime?
  removedAt DateTime?

  // Relations
  post             Post?             @relation(fields: [postId], references: [id])
  postId           Int?
  comment          Comment?          @relation(fields: [commentId], references: [id], name: "CommentToComments")
  commentId        Int?
  game             Game?             @relation(fields: [gameId], references: [id], name: "CommentToGame")
  gameId           Int?
  gamePage         GamePage?         @relation(fields: [gamePageId], references: [id], name: "CommentToGamePage")
  gamePageId       Int?
  track            GamePageTrack?    @relation(fields: [trackId], references: [id], name: "CommentToTrack")
  trackId          Int?
  author           User              @relation(fields: [authorId], references: [id])
  authorId         Int
  likes            Like[]            @relation(name: "LikeToComment")
  commentReactions CommentReaction[]
  reports          Report[]          @relation(name: "ReportToComment")
  children         Comment[]         @relation(name: "CommentToComments")
  notifications    Notification[]    @relation(name: "NotificationToComment")
}

model Jam {
  id                     Int      @id @default(autoincrement())
  name                   String
  slug                   String   @unique
  startTime              DateTime @default(now())
  suggestionHours        Int      @default(168)
  slaughterHours         Int      @default(96)
  votingHours            Int      @default(72)
  jammingHours           Int      @default(72)
  ratingHours            Int      @default(334)
  submissionHours        Int      @default(2)
  postJamRefinementHours Int      @default(336)
  postJamRatingHours     Int      @default(336)
  isActive               Boolean  @default(true)
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt
  themePerUser           Int      @default(3)
  themePerRound          Int      @default(15)
  noOfRounds             Int      @default(1)
  icon                   String?
  color                  String?
  tenantId               String?  @map("tenant_id")
  sourceUrl              String?  @unique @map("source_url")
  sourcePlatform         String?  @map("source_platform")

  // Relations
  games            Game[]            @relation(name: "GameToJam")
  users            User[]
  themeSuggestions ThemeSuggestion[] @relation(name: "SuggestionToJam")
  themeVotes       ThemeVote[]       @relation(name: "VoteToJam")
  themeVotes2      ThemeVote2[]      @relation(name: "Vote2ToJam")
  teams            Team[]            @relation(name: "TeamsToJam")

  @@index([tenantId, startTime])
}

model Event {
  id        Int      @id @default(autoincrement())
  slug      String   @unique
  name      String
  content   String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  startTime DateTime
  endTime   DateTime
  link      String?
  icon      String?

  // Relations
  category   EventCategory @relation(fields: [categoryId], references: [id])
  categoryId Int
  host       User          @relation(fields: [hostId], references: [id])
  hostId     Int
}

model EventCategory {
  id        Int      @id @default(autoincrement())
  name      String   @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relation
  events Event[]
}

enum GameCategory {
  REGULAR
  ODA
  EXTRA
  EXTERNAL
}

model Game {
  id                  Int              @id @default(autoincrement())
  slug                String           @unique
  category            GameCategory
  createdAt           DateTime         @default(now())
  updatedAt           DateTime         @updatedAt
  ratingCategories    RatingCategory[]
  majRatingCategories RatingCategory[] @relation(name: "GameToMajorityContentRating")
  published           Boolean          @default(false)
  publishedAt         DateTime?        @map("published_at")
  tenantId            String?          @map("tenant_id")
  sourceUrl           String?          @unique @map("source_url")
  sourcePlatform      String?          @map("source_platform")

  // Relations
  team               Team               @relation(fields: [teamId], references: [id], name: "GameToTeam")
  teamId             Int                @unique
  jam                Jam                @relation(fields: [jamId], references: [id], name: "GameToJam")
  jamId              Int
  reports            Report[]           @relation(name: "ReportToGame")
  downloadLinks      GameDownloadLink[] @relation(name: "LinkToGame")
  ratings            Rating[]           @relation(name: "RatingToGame")
  flags              Flag[]             @relation(name: "GamesToFlags")
  tags               Tag[]              @relation(name: "GamesToTags")
  comments           Comment[]          @relation(name: "CommentToGame")
  notifications      Notification[]     @relation(name: "NotificationToGame")
  streamerAlerts     StreamerGameAlert[] @relation(name: "StreamerGameAlertToGame")
  gameEmotes         Reaction[]         @relation(name: "ReactionToGameOwner")
  recommendedByUsers User[]             @relation(name: "UserRecommendedGames")
  pages              GamePage[]         @relation(name: "GameToPages")

  @@index([tenantId, jamId])
  @@index([published, publishedAt])
}

model GamePage {
  id                   Int         @id @default(autoincrement())
  version              PageVersion
  name                 String
  description          String?
  short                String?
  thumbnail            String?
  soundtrackThumbnail  String?
  banner               String?
  screenshots          String[]    @default([])
  trailerUrl           String?
  itchEmbedUrl         String?
  itchEmbedAspectRatio String?
  playableBuildUrl     String?
  playableBuildAspectRatio String?
  playableBuildShowFullscreenButton Boolean @default(true)
  playableBuildId      String?
  inputMethods         String[]    @default([])
  estOneRun            String?
  estAnyPercent        String?
  estHundredPercent    String?
  createdAt            DateTime    @default(now())
  updatedAt            DateTime    @updatedAt
  themeJustification   String?
  emotePrefix          String?

  game                Game                   @relation(fields: [gameId], references: [id], name: "GameToPages")
  playableBuild       WebBuild?               @relation(fields: [playableBuildId], references: [id], onDelete: SetNull)
  gameId              Int
  ratingCategories    RatingCategory[]
  majRatingCategories RatingCategory[]       @relation(name: "GamePageToMajorityContentRating")
  flags               Flag[]                 @relation(name: "GamePagesToFlags")
  tags                Tag[]                  @relation(name: "GamePagesToTags")
  downloadLinks       GamePageDownloadLink[] @relation(name: "LinkToGamePage")
  tracks              GamePageTrack[]        @relation(name: "TrackToGamePage")
  leaderboards        GamePageLeaderboard[]  @relation(name: "LeaderboardToGamePage")
  achievements        GamePageAchievement[]  @relation(name: "AchievementToGamePage")
  comments            Comment[]              @relation(name: "CommentToGamePage")
  ghosts              Ghost[]                @relation(name: "GhostToGamePage")
  data                Data[]                 @relation(name: "DataToGamePage")
  ratings             Rating[]               @relation(name: "RatingToGamePage")

  @@unique([gameId, version])
  @@index([playableBuildId])
}

model WebBuild {
  id            String    @id
  ownerId       Int
  archiveBytes  Int
  expandedBytes Int
  fileCount     Int
  scanned       Boolean   @default(false)
  createdAt     DateTime  @default(now())
  claimedAt     DateTime?
  deleteAfter   DateTime?

  owner User @relation(name: "WebBuildToOwner", fields: [ownerId], references: [id], onDelete: Cascade)
  gamePages GamePage[]

  @@index([ownerId, createdAt])
  @@index([claimedAt, createdAt])
  @@index([deleteAfter])
}

model Flag {
  id          Int      @id @default(autoincrement())
  name        String
  description String?
  icon        String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  // Relations
  games     Game[]     @relation(name: "GamesToFlags")
  gamePages GamePage[] @relation(name: "GamePagesToFlags")
}

model RatingCategory {
  id                 Int      @id @default(autoincrement())
  name               String
  description        String?
  askMajorityContent Boolean  @default(false)
  order              Int      @default(0)
  always             Boolean  @default(false)
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  // Relations
  ratings             Rating[]   @relation("RatingToCategory")
  games               Game[]
  majContentGames     Game[]     @relation("GameToMajorityContentRating")
  gamePages           GamePage[]
  majContentGamePages GamePage[] @relation("GamePageToMajorityContentRating")
}

model TrackRatingCategory {
  id          Int      @id @default(autoincrement())
  name        String   @unique
  description String?
  order       Int      @default(0)
  always      Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  ratings TrackRating[] @relation("TrackRatingToCategory")
}

model TrackTagCategory {
  id        Int      @id @default(autoincrement())
  name      String   @unique
  priority  Int      @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tags TrackTag[]
}

model TrackTag {
  id          Int      @id @default(autoincrement())
  name        String   @unique
  description String?
  icon        String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  category       TrackTagCategory @relation(fields: [categoryId], references: [id])
  categoryId     Int
  gamePageTracks GamePageTrack[]
}

model TrackFlag {
  id          Int      @id @default(autoincrement())
  name        String   @unique
  description String?
  icon        String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  gamePageTracks GamePageTrack[]
}

model Rating {
  id        Int      @id @default(autoincrement())
  value     Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relations
  category   RatingCategory @relation(fields: [categoryId], references: [id], name: "RatingToCategory")
  categoryId Int
  user       User           @relation(fields: [userId], references: [id], name: "RatingToUser")
  userId     Int
  game       Game           @relation(fields: [gameId], references: [id], name: "RatingToGame")
  gameId     Int
  gamePage   GamePage       @relation(fields: [gamePageId], references: [id], name: "RatingToGamePage")
  gamePageId Int

  @@unique([gamePageId, categoryId, userId])
}

model TrackRating {
  id        Int      @id @default(autoincrement())
  value     Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  category   TrackRatingCategory @relation(fields: [categoryId], references: [id], name: "TrackRatingToCategory")
  categoryId Int
  user       User                @relation(fields: [userId], references: [id], name: "TrackRatingToUser")
  userId     Int
  track      GamePageTrack       @relation(fields: [trackId], references: [id], name: "TrackRatingToTrack")
  trackId    Int

  @@unique([trackId, categoryId, userId])
}

model TrackTimestampComment {
  id        Int      @id @default(autoincrement())
  content   String
  timestamp Float
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  author   User          @relation(fields: [authorId], references: [id], name: "TrackTimestampCommentToAuthor")
  authorId Int
  track    GamePageTrack @relation(fields: [trackId], references: [id], name: "TrackTimestampCommentToTrack")
  trackId  Int
}

model GamePageTrack {
  id                            Int      @id @default(autoincrement())
  slug                          String
  url                           String
  name                          String
  license                       String?
  allowDownload                 Boolean  @default(false)
  allowBackgroundUse            Boolean  @default(false)
  allowBackgroundUseAttribution Boolean  @default(false)
  bpm                           Int?
  musicalKey                    String?
  integratedLufs                Float?
  truePeakDb                    Float?
  loudnessGainDb                Float?
  softwareUsed                  String[] @default([])
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  commentary                    String?

  composer           User                    @relation(fields: [composerId], references: [id], name: "GamePageTrackToComposer")
  composerId         Int
  gamePage           GamePage                @relation(fields: [gamePageId], references: [id], name: "TrackToGamePage")
  gamePageId         Int
  tags               TrackTag[]
  flags              TrackFlag[]
  links              GamePageTrackLink[]     @relation(name: "GamePageTrackToLinks")
  credits            GamePageTrackCredit[]   @relation(name: "GamePageTrackToCredits")
  comments           Comment[]               @relation(name: "CommentToTrack")
  ratings            TrackRating[]           @relation(name: "TrackRatingToTrack")
  notifications      Notification[]          @relation(name: "NotificationToTrack")
  timestampComments  TrackTimestampComment[] @relation(name: "TrackTimestampCommentToTrack")
  recommendedByUsers User[]                  @relation(name: "UserRecommendedTracks")

  @@unique([gamePageId, slug])
}

model GamePageTrackLink {
  id        Int      @id @default(autoincrement())
  label     String
  url       String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  track   GamePageTrack @relation(fields: [trackId], references: [id], name: "GamePageTrackToLinks", onDelete: Cascade)
  trackId Int
}

model GamePageTrackCredit {
  id        Int      @id @default(autoincrement())
  role      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  track   GamePageTrack @relation(fields: [trackId], references: [id], name: "GamePageTrackToCredits", onDelete: Cascade)
  trackId Int
  user    User          @relation(fields: [userId], references: [id], name: "GamePageTrackCreditToUser")
  userId  Int
}

enum LeaderboardType {
  SCORE
  GOLF
  SPEEDRUN
  ENDURANCE
}

model GamePageLeaderboard {
  id            Int             @id @default(autoincrement())
  type          LeaderboardType
  name          String
  decimalPlaces Int             @default(0)
  maxUsersShown Int             @default(10)
  onlyBest      Boolean         @default(true)
  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt

  gamePage   GamePage @relation(fields: [gamePageId], references: [id], name: "LeaderboardToGamePage")
  gamePageId Int
  scores     Score[]  @relation(name: "ScoreToGamePageLeaderboard")
}

model Score {
  id        Int      @id @default(autoincrement())
  data      Int
  evidence  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relations
  user          User                @relation(fields: [userId], references: [id], name: "ScoreToUser")
  userId        Int
  leaderboard   GamePageLeaderboard @relation(fields: [leaderboardId], references: [id], name: "ScoreToGamePageLeaderboard")
  leaderboardId Int
}

model GamePageAchievement {
  id          Int      @id @default(autoincrement())
  name        String
  description String?
  image       String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  users      User[]   @relation(name: "GamePageAchievementToUsers")
  unlocks    AchievementUnlock[] @relation(name: "AchievementUnlockToAchievement")
  gamePage   GamePage @relation(fields: [gamePageId], references: [id], name: "AchievementToGamePage")
  gamePageId Int
}

model AchievementUnlock {
  achievementId Int
  userId        Int
  earnedAt      DateTime @default(now())

  achievement GamePageAchievement @relation(name: "AchievementUnlockToAchievement", fields: [achievementId], references: [id], onDelete: Cascade)
  user        User                @relation(name: "AchievementUnlockToUser", fields: [userId], references: [id], onDelete: Cascade)

  @@id([achievementId, userId])
  @@index([earnedAt(sort: Desc)])
  @@index([userId])
}

model Ghost {
  id        Int      @id @default(autoincrement())
  data      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relations
  user       User     @relation(fields: [userId], references: [id], name: "GhostToUser")
  userId     Int
  gamePage   GamePage @relation(fields: [gamePageId], references: [id], name: "GhostToGamePage")
  gamePageId Int
}

model Data {
  id        Int      @id @default(autoincrement())
  data      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relations
  user       User     @relation(fields: [userId], references: [id], name: "DataToUser")
  userId     Int
  gamePage   GamePage @relation(fields: [gamePageId], references: [id], name: "DataToGamePage")
  gamePageId Int
}

model GameDownloadLink {
  id       Int    @id @default(autoincrement())
  url      String
  platform String // Will store: "Windows", "MacOS", "Linux", "Web", "Mobile", "Other"

  // Relation to Game
  game   Game @relation(fields: [gameId], references: [id], onDelete: Cascade, name: "LinkToGame")
  gameId Int
}

model GamePageDownloadLink {
  id       Int    @id @default(autoincrement())
  url      String
  platform String

  gamePage   GamePage @relation(fields: [gamePageId], references: [id], onDelete: Cascade, name: "LinkToGamePage")
  gamePageId Int
}

model Like {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relations
  user      User     @relation(fields: [userId], references: [id])
  userId    Int
  post      Post?    @relation(fields: [postId], references: [id], name: "LikeToPost")
  postId    Int?
  comment   Comment? @relation(fields: [commentId], references: [id], name: "LikeToComment")
  commentId Int?
}

model FeaturedStreamer {
  id           Int      @id @default(autoincrement())
  userName     String
  thumbnailUrl String
  streamTitle  String
  streamTags   String[] @default([])
  viewerCount  Int      @default(0)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model Report {
  id         Int       @id @default(autoincrement())
  resolved   Boolean   @default(false)
  status     String    @default("open")
  priority   String    @default("normal")
  reason     String?
  details    String?
  resolvedAt DateTime? @map("resolved_at")
  resolution String?
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt

  // Relations
  reporter            User     @relation(fields: [reporterId], references: [id], name: "ReportToReporter")
  reporterId          Int
  user                User?    @relation(fields: [userId], references: [id], name: "ReportToUser")
  userId              Int?
  post                Post?    @relation(fields: [postId], references: [id], name: "ReportToPost")
  postId              Int?
  comment             Comment? @relation(fields: [commentId], references: [id], name: "ReportToComment")
  commentId           Int?
  game                Game?    @relation(fields: [gameId], references: [id], name: "ReportToGame")
  gameId              Int?
  assignedTo          User?    @relation(fields: [assignedToId], references: [id], name: "ReportToAssignee")
  assignedToId        Int?     @map("assigned_to_id")
  collectionCommentId String?  @map("collection_comment_id")
  directMessage        ConversationMessage? @relation(fields: [directMessageId], references: [id], name: "ReportToDirectMessage")
  directMessageId      Int?     @map("direct_message_id")

  @@index([directMessageId])
}

enum ConversationType {
  DIRECT
  GROUP
}

enum ConversationMemberStatus {
  PENDING
  ACCEPTED
  DECLINED
}

enum ConversationMemberRole {
  OWNER
  MEMBER
}

enum MessageRequestPolicy {
  EVERYONE
  FOLLOWING
  NOBODY
}

model Conversation {
  id            Int              @id @default(autoincrement())
  type          ConversationType @default(DIRECT)
  name          String?
  createdBy     User             @relation(fields: [createdById], references: [id], name: "ConversationCreator")
  createdById   Int
  tenantId      String?          @map("tenant_id")
  createdAt     DateTime         @default(now())
  updatedAt     DateTime         @updatedAt
  lastMessageAt DateTime         @default(now()) @map("last_message_at")
  members       ConversationMember[]
  messages      ConversationMessage[]

  @@index([tenantId, lastMessageAt])
  @@index([createdById, lastMessageAt])
}

model ConversationMember {
  conversation    Conversation             @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  conversationId  Int
  user            User                     @relation(fields: [userId], references: [id], name: "ConversationMemberUser", onDelete: Cascade)
  userId          Int
  invitedBy       User?                    @relation(fields: [invitedById], references: [id], name: "ConversationMemberInviter", onDelete: SetNull)
  invitedById     Int?                     @map("invited_by_id")
  status          ConversationMemberStatus @default(PENDING)
  role            ConversationMemberRole   @default(MEMBER)
  requestMessage  ConversationMessage?     @relation(fields: [requestMessageId], references: [id], name: "ConversationRequestMessage", onDelete: SetNull)
  requestMessageId Int?                    @map("request_message_id")
  lastReadMessage ConversationMessage?     @relation(fields: [lastReadMessageId], references: [id], name: "ConversationLastReadMessage", onDelete: SetNull)
  lastReadMessageId Int?                   @map("last_read_message_id")
  mutedAt         DateTime?                @map("muted_at")
  archivedAt      DateTime?                @map("archived_at")
  joinedAt        DateTime?                @map("joined_at")
  createdAt       DateTime                 @default(now())
  updatedAt       DateTime                 @updatedAt

  @@id([conversationId, userId])
  @@index([userId, status, archivedAt])
}

model ConversationMessage {
  id             Int                  @id @default(autoincrement())
  conversation   Conversation         @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  conversationId Int                  @map("conversation_id")
  sender         User                 @relation(fields: [senderId], references: [id], name: "ConversationMessageSender")
  senderId       Int                  @map("sender_id")
  body           String               @db.VarChar(4000)
  createdAt      DateTime             @default(now())
  deletedAt      DateTime?            @map("deleted_at")
  requestFor     ConversationMember[] @relation(name: "ConversationRequestMessage")
  readBy         ConversationMember[] @relation(name: "ConversationLastReadMessage")
  reports        Report[]             @relation(name: "ReportToDirectMessage")

  @@index([conversationId, createdAt])
  @@index([senderId, createdAt])
}

model UserBlock {
  blocker   User     @relation(fields: [blockerId], references: [id], name: "UserBlockBlocker", onDelete: Cascade)
  blockerId Int      @map("blocker_id")
  blocked   User     @relation(fields: [blockedId], references: [id], name: "UserBlockBlocked", onDelete: Cascade)
  blockedId Int      @map("blocked_id")
  createdAt DateTime @default(now())

  @@id([blockerId, blockedId])
  @@index([blockedId, blockerId])
}

enum NotificationType {
  GENERAL
  GAME_COMMENT
  TRACK_COMMENT
  COMMENT_REPLY
  POST_COMMENT
  FOLLOW
  FOLLOW_BACK
  TEAM_INVITE
  TEAM_APPLICATION
  STREAMER_LIVE
}

model Notification {
  id   Int              @id @default(autoincrement())
  type NotificationType

  title      String?
  body       String?
  link       String?
  data       Json?
  readAt     DateTime? @map("read_at")
  archivedAt DateTime? @map("archived_at")
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt

  // who triggered it
  actor   User? @relation(fields: [actorId], references: [id], name: "NotificationToActor")
  actorId Int?

  // who receives it
  recipient   User @relation(fields: [recipientId], references: [id], name: "NotificationToRecipient")
  recipientId Int

  // optional targets
  post              Post?            @relation(name: "post", fields: [postId], references: [id])
  postId            Int?
  comment           Comment?         @relation(name: "NotificationToComment", fields: [commentId], references: [id])
  commentId         Int?
  game              Game?            @relation(name: "NotificationToGame", fields: [gameId], references: [id])
  gameId            Int?
  track             GamePageTrack?   @relation(name: "NotificationToTrack", fields: [trackId], references: [id])
  trackId           Int?
  team              Team?            @relation(name: "team", fields: [teamId], references: [id])
  teamId            Int?
  teamInvite        TeamInvite?      @relation(name: "teamInvite", fields: [teamInviteId], references: [id])
  teamInviteId      Int?
  teamApplication   TeamApplication? @relation(name: "teamApplication", fields: [teamApplicationId], references: [id])
  teamApplicationId Int?

  @@index([type, recipientId, createdAt])
  @@index([postId])
  @@index([commentId])
  @@index([gameId])
  @@index([trackId])
  @@index([teamId])
}

model ThemeSuggestion {
  id          Int       @id @default(autoincrement())
  suggestion  String
  description String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime? @updatedAt

  // Relations
  createdBy  User         @relation(fields: [userId], references: [id], name: "SuggestionToUser")
  userId     Int
  createdFor Jam          @relation(fields: [jamId], references: [id], name: "SuggestionToJam")
  jamId      Int
  votes      ThemeVote[]
  votes2     ThemeVote2[]
}

model ThemeVote {
  id             Int       @id @default(autoincrement())
  slaughterScore Int       @default(0)
  createdAt      DateTime  @default(now())
  updatedAt      DateTime? @updatedAt

  // Relations
  user              User            @relation(fields: [userId], references: [id], name: "VoteToUser")
  userId            Int
  jam               Jam             @relation(fields: [jamId], references: [id], name: "VoteToJam")
  jamId             Int
  themeSuggestion   ThemeSuggestion @relation(fields: [themeSuggestionId], references: [id])
  themeSuggestionId Int
}

model Team {
  id               Int        @id @default(autoincrement())
  name             String?
  applicationsOpen Boolean    @default(false)
  rolesWanted      TeamRole[]
  description      String?
  tenantId         String?    @map("tenant_id")

  // Relations
  owner         User              @relation(fields: [ownerId], references: [id], name: "TeamsToOwner")
  ownerId       Int
  users         User[]
  jam           Jam               @relation(fields: [jamId], references: [id], name: "TeamsToJam")
  jamId         Int
  invites       TeamInvite[]      @relation(name: "TeamInvitesToTeam")
  applications  TeamApplication[] @relation(name: "TeamApplicationsToTeam")
  game          Game?             @relation(name: "GameToTeam")
  notifications Notification[]    @relation(name: "team")

  @@index([tenantId, jamId])
}

model TeamRole {
  id          Int       @id @default(autoincrement())
  slug        String    @unique
  name        String
  description String
  icon        String
  createdAt   DateTime  @default(now())
  updatedAt   DateTime? @updatedAt

  // Relations
  primaryUsers   User[] @relation(name: "PrimaryRolesToUsers")
  secondaryUsers User[] @relation(name: "SecondaryRolesToUsers")
  wantedBy       Team[]
}

model TeamInvite {
  id        Int       @id @default(autoincrement())
  content   String?
  createdAt DateTime  @default(now())
  updatedAt DateTime? @updatedAt

  //Relations
  team          Team           @relation(fields: [teamId], references: [id], name: "TeamInvitesToTeam")
  teamId        Int
  user          User           @relation(fields: [userId], references: [id], name: "TeamInvitesToUser")
  userId        Int
  notifications Notification[] @relation(name: "teamInvite")
}

model TeamApplication {
  id        Int       @id @default(autoincrement())
  content   String?
  createdAt DateTime  @default(now())
  updatedAt DateTime? @updatedAt

  //Relations
  team          Team           @relation(fields: [teamId], references: [id], name: "TeamApplicationsToTeam")
  teamId        Int
  user          User           @relation(fields: [userId], references: [id], name: "TeamApplicationsToUser")
  userId        Int
  notifications Notification[] @relation(name: "teamApplication")
}

model ThemeVote2 {
  id        Int       @id @default(autoincrement())
  voteScore Int
  voteRound Int
  createdAt DateTime  @default(now())
  updatedAt DateTime? @updatedAt

  // Relations
  user              User            @relation(fields: [userId], references: [id], name: "Vote2ToUser")
  userId            Int
  jam               Jam             @relation(fields: [jamId], references: [id], name: "Vote2ToJam")
  jamId             Int
  themeSuggestion   ThemeSuggestion @relation(fields: [themeSuggestionId], references: [id])
  themeSuggestionId Int
}

model ServiceKey {
  id           String    @id
  name         String
  scopes       Json
  tenantId     String?   @map("tenant_id")
  createdAt    DateTime  @default(now()) @map("created_at")
  updatedAt    DateTime  @default(now()) @map("updated_at")
  deprecatedAt DateTime? @map("deprecated_at")
  revokedAt    DateTime? @map("revoked_at")
  lastUsedAt   DateTime? @map("last_used_at")
  keyPrefix    String    @map("key_prefix")
  keyHash      String    @map("key_hash")
  usageCount   Int       @default(0) @map("usage_count")
}

model PlatformJob {
  id        String    @id
  type      String
  payload   Json
  status    String
  attempts  Int       @default(0)
  runAt     DateTime  @map("run_at")
  createdAt DateTime  @default(now()) @map("created_at")
  updatedAt DateTime  @default(now()) @map("updated_at")
  lastError String?   @map("last_error")
  lockedAt  DateTime? @map("locked_at")
  lockToken String?   @map("lock_token")

  @@index([status, runAt])
}

model IdempotencyRecord {
  idempotencyKey String   @id @map("idempotency_key")
  requestHash    String   @map("request_hash")
  status         String
  responseStatus Int?     @map("response_status")
  responseKind   String?  @map("response_kind")
  responseBody   Json?    @map("response_body")
  createdAt      DateTime @default(now()) @map("created_at")
  updatedAt      DateTime @default(now()) @map("updated_at")
  expiresAt      DateTime @map("expires_at")

  @@index([expiresAt])
}

model DomainEvent {
  eventId    String   @id @map("event_id")
  type       String
  tenantId   String?  @map("tenant_id")
  payload    Json
  occurredAt DateTime @map("occurred_at")

  @@index([occurredAt])
}

model WebhookSubscription {
  id             String    @id
  endpoint       String
  events         Json
  secret         String?
  headers        Json?
  tenantId       String?   @map("tenant_id")
  status         String    @default("active")
  createdAt      DateTime  @default(now()) @map("created_at")
  updatedAt      DateTime  @default(now()) @map("updated_at")
  lastDeliveryAt DateTime? @map("last_delivery_at")
  failureCount   Int       @default(0) @map("failure_count")
}

model EventCheckpoint {
  consumerId     String    @id @map("consumer_id")
  lastEventId    String?   @map("last_event_id")
  lastOccurredAt DateTime? @map("last_occurred_at")
  tenantId       String?   @map("tenant_id")
  updatedAt      DateTime  @default(now()) @map("updated_at")
}

model RoleGrant {
  id           String   @id
  subjectType  String   @map("subject_type")
  subjectId    String   @map("subject_id")
  role         String
  tenantId     String?  @map("tenant_id")
  resourceType String?  @map("resource_type")
  resourceId   String?  @map("resource_id")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @default(now()) @map("updated_at")
}

model SearchSynonym {
  id        String   @id
  tenantId  String?  @map("tenant_id")
  term      String
  synonym   String
  groupKey  String?  @map("group_key")
  notes     String?
  enabled   Boolean  @default(true)
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @map("updated_at")
}

model SearchSettings {
  tenantId               String   @id @map("tenant_id")
  exactMatchBoost        Float    @default(3) @map("exact_match_boost")
  prefixMatchBoost       Float    @default(2) @map("prefix_match_boost")
  substringMatchBoost    Float    @default(1) @map("substring_match_boost")
  fuzzyThreshold         Float    @default(0.1) @map("fuzzy_threshold")
  gameWeight             Float    @default(1.2) @map("game_weight")
  trackWeight            Float    @default(1) @map("track_weight")
  postWeight             Float    @default(1) @map("post_weight")
  userWeight             Float    @default(1) @map("user_weight")
  teamWeight             Float    @default(0.9) @map("team_weight")
  freshnessHalfLifeHours Int      @default(168) @map("freshness_half_life_hours")
  updatedAt              DateTime @default(now()) @map("updated_at")
}

model SearchDocument {
  documentId      String   @id @map("document_id")
  tenantId        String   @map("tenant_id")
  entityType      String   @map("entity_type")
  entityId        Int      @map("entity_id")
  variant         String?
  title           String
  subtitle        String?
  body            String
  slug            String?
  tags            Json
  visibility      String
  metadata        Json
  sourceUpdatedAt DateTime @map("source_updated_at")
  indexedAt       DateTime @default(now()) @map("indexed_at")

  @@index([tenantId, entityType, entityId])
}

model SearchReindexRun {
  id             String    @id
  tenantId       String?   @map("tenant_id")
  scope          String
  status         String
  batchSize      Int       @map("batch_size")
  entityTypes    Json      @map("entity_types")
  perEntityState Json      @map("per_entity_state")
  totalCount     Int       @map("total_count")
  processedCount Int       @default(0) @map("processed_count")
  lastError      String?   @map("last_error")
  createdAt      DateTime  @default(now()) @map("created_at")
  updatedAt      DateTime  @default(now()) @map("updated_at")
  startedAt      DateTime? @map("started_at")
  completedAt    DateTime? @map("completed_at")
}

model PostRevision {
  id        String   @id
  postId    Int      @map("post_id")
  editorId  Int?     @map("editor_id")
  title     String
  content   String
  sticky    Boolean  @default(false)
  tags      Json?
  createdAt DateTime @default(now()) @map("created_at")

  @@index([postId, createdAt])
}

model Collection {
  id           Int      @id @default(autoincrement())
  tenantId     String?  @map("tenant_id")
  ownerId      Int      @map("owner_id")
  slug         String
  title        String
  description  String?
  collectionType String @default("music") @map("collection_type")
  visibility   String   @default("private")
  forkedFromId Int?     @map("forked_from_id")
  playbackMode String   @default("manual") @map("playback_mode")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @default(now()) @map("updated_at")

  @@unique([tenantId, slug])
  @@index([tenantId, visibility, updatedAt])
}

model CollectionItem {
  id            Int      @id @default(autoincrement())
  collectionId  Int      @map("collection_id")
  itemType      String   @map("item_type")
  itemId        Int      @map("item_id")
  title         String?
  url           String?
  thumbnailUrl  String?  @map("thumbnail_url")
  platformLinks Json?    @map("platform_links")
  note          String?
  position      Int      @default(0)
  addedAt       DateTime @default(now()) @map("added_at")

  @@unique([collectionId, itemType, itemId])
  @@index([collectionId, position, addedAt])
  @@index([itemType, itemId])
}

model CollectionCollaborator {
  id           Int      @id @default(autoincrement())
  collectionId Int      @map("collection_id")
  userId       Int      @map("user_id")
  role         String   @default("viewer")
  status       String   @default("pending")
  invitedBy    Int?     @map("invited_by")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @default(now()) @map("updated_at")

  @@unique([collectionId, userId])
}

model CollectionComment {
  id           Int       @id @default(autoincrement())
  collectionId Int       @map("collection_id")
  authorId     Int       @map("author_id")
  content      String
  deletedAt    DateTime? @map("deleted_at")
  createdAt    DateTime  @default(now()) @map("created_at")
  updatedAt    DateTime  @default(now()) @map("updated_at")

  @@index([collectionId, createdAt])
}

model CollectionFollow {
  collectionId Int      @map("collection_id")
  userId       Int      @map("user_id")
  createdAt    DateTime @default(now()) @map("created_at")

  @@id([collectionId, userId])
  @@index([userId, createdAt])
}

model CollectionImport {
  id           Int      @id @default(autoincrement())
  collectionId Int      @map("collection_id")
  importedBy   Int      @map("imported_by")
  sourceName   String?  @map("source_name")
  sourceFormat String   @default("jamcore") @map("source_format")
  importedAt   DateTime @default(now()) @map("imported_at")
}

model FederationBlock {
  id        String   @id
  tenantId  String?  @map("tenant_id")
  blockType String   @map("block_type")
  value     String
  reason    String?
  createdBy Int?     @map("created_by")
  createdAt DateTime @default(now()) @map("created_at")

  @@unique([tenantId, blockType, value])
}

model FederationTrustSetting {
  tenantId  String   @id @map("tenant_id")
  mode      String   @default("open")
  updatedAt DateTime @default(now()) @map("updated_at")
}

model FederationAllowlistEntry {
  id        String   @id
  tenantId  String?  @map("tenant_id")
  allowType String   @map("allow_type")
  value     String
  createdBy Int?     @map("created_by")
  createdAt DateTime @default(now()) @map("created_at")

  @@unique([tenantId, allowType, value])
}

model FederationReputation {
  id                  String   @id
  tenantId            String?  @map("tenant_id")
  host                String
  trustLevel          String   @default("unknown") @map("trust_level")
  deliveriesFailed    Int      @default(0) @map("deliveries_failed")
  deliveriesSucceeded Int      @default(0) @map("deliveries_succeeded")
  rejectedActivities  Int      @default(0) @map("rejected_activities")
  acceptedActivities  Int      @default(0) @map("accepted_activities")
  reportCount         Int      @default(0) @map("report_count")
  notes               String?
  updatedBy           Int?     @map("updated_by")
  createdAt           DateTime @default(now()) @map("created_at")
  updatedAt           DateTime @default(now()) @map("updated_at")

  @@unique([tenantId, host])
}

model FederationPreviewQueueItem {
  id           String    @id
  tenantId     String?   @map("tenant_id")
  actorId      String    @map("actor_id")
  activityId   String?   @map("activity_id")
  activityType String?   @map("activity_type")
  objectType   String?   @map("object_type")
  summary      String?
  payload      Json
  status       String    @default("pending")
  reviewedBy   Int?      @map("reviewed_by")
  reviewedAt   DateTime? @map("reviewed_at")
  createdAt    DateTime  @default(now()) @map("created_at")

  @@index([tenantId, status, createdAt])
}

model RadioSession {
  tenantId        String    @id @map("tenant_id")
  enabled         Boolean   @default(true)
  currentTrackId  Int?      @map("current_track_id")
  startedAt       DateTime? @map("started_at")
  durationSeconds Int       @default(180) @map("duration_seconds")
  voteRound       String    @map("vote_round")
  voteOptions     Json      @map("vote_options")
  history         Json
  updatedAt       DateTime  @default(now()) @map("updated_at")
}

model RadioVote {
  id        String   @id
  tenantId  String   @map("tenant_id")
  voteRound String   @map("vote_round")
  userId    Int      @map("user_id")
  trackId   Int      @map("track_id")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @map("updated_at")

  @@unique([tenantId, voteRound, userId])
  @@index([tenantId, voteRound, trackId])
}

model RadioEmote {
  id        String   @id
  tenantId  String   @map("tenant_id")
  userId    Int      @map("user_id")
  emote     String
  x         Float?
  y         Float?
  createdAt DateTime @default(now()) @map("created_at")

  @@index([tenantId, createdAt])
}

model RadioBan {
  id        String   @id
  tenantId  String   @map("tenant_id")
  trackId   Int      @map("track_id")
  reason    String?
  createdBy Int?     @map("created_by")
  createdAt DateTime @default(now()) @map("created_at")

  @@unique([tenantId, trackId])
}

model Quilt {
  id          Int      @id @default(autoincrement())
  slug        String   @unique
  name        String
  description String?
  width       Int
  height      Int
  reviewWindowMinutes Int      @default(60) @map("review_window_minutes")
  endsAt      DateTime @map("ends_at")
  tenantId    String?  @map("tenant_id")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  submissions QuiltSubmission[]

  @@index([tenantId, endsAt])
}

model QuiltSubmission {
  id          Int                    @id @default(autoincrement())
  quiltId     Int                    @map("quilt_id")
  authorId    Int                    @map("author_id")
  kind        QuiltSubmissionKind    @default(PIXELS)
  pixels      Json
  canvasWidth Int?                   @map("canvas_width")
  canvasHeight Int?                  @map("canvas_height")
  resizeFromWidth Int?               @map("resize_from_width")
  resizeFromHeight Int?              @map("resize_from_height")
  resizeOffsetX Int?                 @map("resize_offset_x")
  resizeOffsetY Int?                 @map("resize_offset_y")
  status      QuiltSubmissionStatus  @default(PENDING)
  resolvesAt  DateTime               @map("resolves_at")
  resolvedAt  DateTime?              @map("resolved_at")
  removedAt   DateTime?              @map("removed_at")
  removedById Int?                   @map("removed_by_id")
  createdAt   DateTime               @default(now()) @map("created_at")
  updatedAt   DateTime               @updatedAt @map("updated_at")

  quilt     Quilt       @relation(fields: [quiltId], references: [id], onDelete: Cascade)
  author    User        @relation(fields: [authorId], references: [id], name: "QuiltSubmissionToAuthor")
  removedBy User?       @relation(fields: [removedById], references: [id], name: "QuiltSubmissionToRemover")
  votes     QuiltVote[]

  @@index([quiltId, status, createdAt])
  @@index([authorId, createdAt])
}

model QuiltVote {
  id           Int      @id @default(autoincrement())
  submissionId Int      @map("submission_id")
  userId       Int      @map("user_id")
  value        Int
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  submission QuiltSubmission @relation(fields: [submissionId], references: [id], onDelete: Cascade)
  user       User            @relation(fields: [userId], references: [id], name: "QuiltVoteToUser")

  @@unique([submissionId, userId])
  @@index([userId, createdAt])
}

model ReportNote {
  id        String   @id
  reportId  Int      @map("report_id")
  authorId  Int      @map("author_id")
  note      String
  createdAt DateTime @default(now()) @map("created_at")

  @@index([reportId, createdAt])
}

model NotificationPreference {
  userId       Int      @id @map("user_id")
  mutedTypes   Json     @map("muted_types")
  emailEnabled Boolean  @default(false) @map("email_enabled")
  updatedAt    DateTime @default(now()) @map("updated_at")

  // Streamer-live alert preferences. Opt-out by default, since the whole
  // point of the feature is for devs to hear about it without setup.
  streamerAlertEmailOptOut     Boolean @default(false) @map("streamer_alert_email_opt_out")
  streamerAlertSoundOptOut     Boolean @default(false) @map("streamer_alert_sound_opt_out")
  streamerAlertSoundUrl        String? @map("streamer_alert_sound_url")
  streamerAlertSoundDurationMs Int?    @map("streamer_alert_sound_duration_ms")
}

model StreamerGameAlert {
  id Int @id @default(autoincrement())

  game   Game @relation(fields: [gameId], references: [id], name: "StreamerGameAlertToGame")
  gameId Int  @map("game_id")

  streamer   User @relation(fields: [streamerId], references: [id], name: "StreamerGameAlertToUser")
  streamerId Int  @map("streamer_id")

  twitchUserId   String   @map("twitch_user_id")
  twitchLogin    String   @map("twitch_login")
  twitchStreamId String   @map("twitch_stream_id")
  startedAt      DateTime @map("started_at")

  notifiedAt DateTime? @map("notified_at")

  // Populated once the stream ends and its VOD is found on Twitch.
  vodId               String?   @map("vod_id")
  vodUrl              String?   @map("vod_url")
  vodTimestampSeconds Int?      @map("vod_timestamp_seconds")
  vodCheckedAt        DateTime? @map("vod_checked_at")
  vodCheckAttempts    Int       @default(0) @map("vod_check_attempts")

  createdAt DateTime @default(now()) @map("created_at")

  @@unique([streamerId, gameId])
  @@index([gameId])
  @@index([vodId])
}

model PostAutosave {
  id        String   @id
  postId    Int?     @map("post_id")
  authorId  Int      @map("author_id")
  tenantId  String?  @map("tenant_id")
  title     String?
  content   String
  sticky    Boolean  @default(false)
  tags      Json?
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @map("updated_at")

  @@unique([tenantId, authorId, postId])
}

model PostGameLink {
  postId       Int      @map("post_id")
  gameId       Int      @map("game_id")
  relationType String   @default("devlog") @map("relation_type")
  createdAt    DateTime @default(now()) @map("created_at")

  @@id([postId, gameId])
  @@index([gameId, createdAt])
}

model PostCollaborator {
  postId    Int      @map("post_id")
  userId    Int      @map("user_id")
  role      String   @default("coauthor")
  status    String   @default("accepted")
  invitedBy Int?     @map("invited_by")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @default(now()) @map("updated_at")

  @@id([postId, userId])
}

model ContentReviewSettings {
  tenantId             String   @id @map("tenant_id")
  requirePostReview    Boolean  @default(false) @map("require_post_review")
  reviewFirstPostsOnly Boolean  @default(false) @map("review_first_posts_only")
  updatedBy            Int?     @map("updated_by")
  updatedAt            DateTime @default(now()) @map("updated_at")
}

model PostSeries {
  id          String   @id
  tenantId    String?  @map("tenant_id")
  ownerId     Int      @map("owner_id")
  slug        String
  title       String
  description String?
  visibility  String   @default("public")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @default(now()) @map("updated_at")

  @@unique([tenantId, ownerId, slug])
  @@index([tenantId, visibility, updatedAt])
}

model PostSeriesPost {
  seriesId String   @map("series_id")
  postId   Int      @map("post_id")
  position Int      @default(0)
  addedAt  DateTime @default(now()) @map("added_at")

  @@id([seriesId, postId])
  @@index([postId])
}

model UserFollow {
  followerId  Int      @map("follower_id")
  followingId Int      @map("following_id")
  tenantId    String?  @map("tenant_id")
  createdAt   DateTime @default(now()) @map("created_at")

  @@id([followerId, followingId])
  @@index([tenantId, followingId, createdAt])
  @@index([tenantId, followerId, createdAt])
}

model RemoteFeedPost {
  id          String    @id
  tenantId    String?   @map("tenant_id")
  activityId  String?   @map("activity_id")
  objectId    String    @map("object_id")
  actorId     String    @map("actor_id")
  actorName   String?   @map("actor_name")
  actorUrl    String?   @map("actor_url")
  sourceHost  String?   @map("source_host")
  title       String?
  content     String
  url         String?
  tags        Json
  sourceType  String    @default("activitypub") @map("source_type")
  status      String    @default("published")
  publishedAt DateTime? @map("published_at")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @default(now()) @map("updated_at")

  @@unique([tenantId, objectId])
  @@index([tenantId, status, publishedAt, createdAt])
}

model RemoteComment {
  id          String    @id
  tenantId    String?   @map("tenant_id")
  activityId  String?   @map("activity_id")
  objectId    String    @map("object_id")
  actorId     String    @map("actor_id")
  actorName   String?   @map("actor_name")
  actorUrl    String?   @map("actor_url")
  sourceHost  String?   @map("source_host")
  content     String
  url         String?
  targetKind  String    @map("target_kind")
  targetId    Int?      @map("target_id")
  targetSlug  String?   @map("target_slug")
  status      String    @default("published")
  publishedAt DateTime? @map("published_at")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @default(now()) @map("updated_at")

  @@unique([tenantId, objectId])
  @@index([tenantId, status, targetKind, targetId], map: "remote_comments_target_id_idx")
  @@index([tenantId, status, targetKind, targetSlug], map: "remote_comments_target_slug_idx")
}
